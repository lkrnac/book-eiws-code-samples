lkrnac/book-eiws-code-samples:

  #The PkgInclude is to identify files that must be included in the application deployment: http://distel.li/pkgincludesection
  PkgInclude:
    - '*'
    #- ["lib/*.jar", "lib/"]

  #The PreRelease section is for specifying build and pre-packaging steps: http://distel.li/prereleasesection
  PreRelease:
    - echo "Begin PreRelease"
    - echo "Installing Java"
    - wget -c -O jdk-8u60-linux-x64.tar.gz --no-check-certificate --no-cookies--header "Cookie: oraclelicense=accept-securebackup-cookie" "http://download.oracle.com/otn-pub/java/jdk/8u60-b27/jdk-8u60-linux-x64.tar.gz"
    - tar -zxvf jdk-8u60-linux-x64.tar.gz
    - cd 0000-examples-parent
    - mvn clean install
    - cd ../0000-examples
    - mvn clean install
    
    

  #The PreInstall section occurs before install: distel.li/preinstallsection
  #PreInstall:
#    - echo "Begin PreInstall"
#    - echo "Installing Java"
#    - 'wget -c -O jre-8u45-linux-x64.tar.gz --no-check-certificate --no-cookies--header "Cookie: oraclelicense=accept-securebackup-cookie" #"http://download.oracle.com/otn-pub/java/jdk/8u45-b14/jre-8u45-linux-x64.tar.gz"'
#    - tar -zxvf jre-8u45-linux-x64.tar.gz

  #The PostInstall section occurs after install: http://distel.li/postinstallsection
  PostInstall:
    - echo "Begin PostInstall"

  #The Exec section executes and monitors the application: distel.li/execsection
  Exec:
    #- java Program
